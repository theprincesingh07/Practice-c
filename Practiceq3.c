// Program to find the ASCII value of a character |
#include<stdio.h>
#include<string.h>
int main(){
    char ch;
    printf("Enter a chararacter to find is ASCII value :- ");
    scanf("%c",&ch);
    printf("ASCII value is %d\n",ch);

return 0;
}